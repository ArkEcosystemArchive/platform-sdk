export enum EntityType {
	Business = 0,
	Product = 1,
	Plugin = 2,
	Module = 3,
	Delegate = 4,
}

export enum EntitySubType {
	None = 0,
	PluginCore = 1,
	PluginDesktop = 2,
}

export enum EntityAction {
	Register = 0,
	Update = 1,
	Resign = 2,
}
