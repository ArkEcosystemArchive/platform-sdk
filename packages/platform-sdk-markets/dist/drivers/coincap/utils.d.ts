/**
 *
 *
 * @param {*} amount
 * @param {*} { from, to, base, rates }
 * @returns {number}
 */
export declare const convertToCurrency: (
	amount: any,
	{
		from,
		to,
		base,
		rates,
	}: {
		from: any;
		to: any;
		base: any;
		rates: any;
	},
) => number;
