import { BigNumber, NumberLike } from "@arkecosystem/platform-sdk-support";
import { ConfigRepository } from "./coins";
import { NetworkHost, NetworkHostType } from "./networks";
export declare const filterHosts: (hosts: NetworkHost[], type: NetworkHostType) => NetworkHost[];
export declare const randomHost: (hosts: NetworkHost[], type: NetworkHostType) => NetworkHost;
export declare const filterHostsFromConfig: (config: ConfigRepository, type: NetworkHostType) => NetworkHost[];
export declare const randomNetworkHostFromConfig: (config: ConfigRepository, type?: NetworkHostType) => NetworkHost;
export declare const randomHostFromConfig: (config: ConfigRepository, type?: NetworkHostType) => string;
export declare const pluckAddress: (query: any) => string;
export declare const toRawUnit: (value: NumberLike, config: ConfigRepository) => BigNumber;
