{
	"name": "@arkecosystem/platform-sdk-http-got",
	"description": "Cross-Platform Utilities for ARK Applications",
	"version": "3.0.26",
	"contributors": [],
	"license": "MIT",
	"files": [
		"/dist"
	],
	"main": "dist/index",
	"types": "dist/index",
	"scripts": {
		"build": "npm run clean && tsc",
		"build:watch": "npm run build -w",
		"clean": "rimraf .coverage dist tmp",
		"test": "jest",
		"test:watch": "jest --watchAll",
		"publish": "npm run build && npm run publish --access public --tolerate-republish"
	},
	"dependencies": {
		"@arkecosystem/platform-sdk": "file:../platform-sdk",
		"form-data": "^3.0.0",
		"got": "^11.8.0"
	},
	"devDependencies": {
		"jest": "^26.6.3",
		"rimraf": "^3.0.2"
	},
	"engines": {
		"node": ">=12.x"
	},
	"jest": {
		"preset": "../../jest.coverage.js"
	}
}
