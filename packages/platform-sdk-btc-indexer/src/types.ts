/**
 * The flags that can be passed in through the CLI.
 *
 * @export
 * @interface Flags
 */
export interface Flags {
	coin: string;
	network: string;
	host: string;
	username: string;
	password: string;
	database: string;
}
